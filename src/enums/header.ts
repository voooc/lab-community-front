export enum HeaderEnum {
    LOGOUT = 'logout',
    MESSAGE_SYSTEM = 'system',
    WRITE_ARTICLE = 'draft-new',
    USER_MAIN = 'user',
}
export enum NotificationEnum {
    SYSTEM = 'system',
    COMMENT = 'comment',
    FOLLOW = 'follow',
    LIKE = 'like',
}
